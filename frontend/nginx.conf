server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # SPA(단일 페이지 앱) 라우팅 처리:
        #   /cart 같은 경로로 새로고침해도 파일이 없으니, index.html을 돌려줘
        #   React Router가 클라이언트에서 경로를 해석하게 한다.
        try_files $uri $uri/ /index.html;
    }
}
